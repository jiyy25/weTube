extends base

block content
    if errorMessage
        span=errorMessage

    form(method="post")
        input(type="text", name="title", placeholder="upload video",required, mexlength=80)
        input(type="text", name="description", placeholder="description",required, minlength=20)
        input(type="text", name="hashtags", placeholder="hashtags",required)
        input(type="submit", value="upload video")